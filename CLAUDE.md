# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目概述

CVIB：在多个数据集（MNIST / ImageNet-100 特征 / Cora / IMDb / AG News BERT 特征 / California Housing / STS-B / ZINC-12k / AgeDB）上对比 MLP / CNN / GCN / RNN 基线与其信息瓶颈变体（VIB / CEB / FGIB / SVIB / NIB / DVCCA）。代码注释、文档字符串和提交信息均为中文。

## 常用命令

```bash
python train.py                     # 默认训练 MLP（100 epochs, 5 runs）
python train.py --model cnn                     # CNN 基线
python train.py --model vib         # VIB（N(0,I) 先验）
python train.py --model svib        # SVIB（Squared-IB，ICLR 2019 Caveats：基于 VIB 的模型变体，CE + β·KL²）
python train.py --backbone cnn --model svib   # CNN 版 SVIB（gnn/rnn 同理）
python train.py --model nib         # NIB（非线性信息瓶颈，Entropy 2019：噪声注入 + 无先验成对距离上界 Î，CE + β·Î；σ² 可学习、初始 1.0）
python train.py --model dvcca       # DVCCA（监督 β-DVCCA，DVMIB JMLR 2025：VIB + 重建解码器，CE + β·KL + β·MSE_recon；仅 MLP/CNN/GNN 骨干）
python train.py --model ceb         # CEB（标签条件先验 r(z|y)，对角高斯）
python train.py --model fgib        # FGIB（固定几何信息瓶颈：确定性主路 + 固定正交锚点先验）
python train.py --model fgib --anchor-scale 8   # 自定义锚点尺度（0 为各类相同锚点）
python train.py --backbone cnn --model vib      # CNN 版 VIB（ceb/fgib 同理）
python train.py --task imagenet100 --model vib      # ImageNet-100 特征分类（MLP 骨干）
python train.py --task imagenet100 --model fgib    # ImageNet-100 特征分类（FGIB）
python train.py --task cora --model gcn             # Cora 节点分类（GCN 基线）
python train.py --task cora --backbone gnn --model vib     # GNN 版 VIB（ceb/fgib 同理）
python train.py --task zinc --model gcn             # ZINC-12k 分子图回归（GCN 基线，官方 10k/1k/1k 划分）
python train.py --task zinc --backbone gnn --model vib     # GNN 版 VIB 图级回归（svib/nib/dvcca 同理）
python train.py --task zinc --backbone gnn --model ceb     # GNN 版 CEB 回归（连续 y 条件先验）
python train.py --task zinc --backbone gnn --model fgib    # GNN 版 FGIB 回归（固定 RFF 连续锚点先验）
python train.py --task imdb --model rnn             # IMDb 情感分析（RNN 基线）
python train.py --task imdb --backbone rnn --model vib     # RNN 版 VIB（ceb/fgib 同理）
python train.py --task agnews --model rnn           # AG News BERT 逐 token 特征分类（RNN 基线）
python train.py --task agnews --backbone rnn --model vib   # RNN 版 VIB（ceb/fgib 同理）
python train.py --task stsb --model rnn             # STS-B 文本相似度回归（RNN 基线）
python train.py --task stsb --backbone rnn --model ceb   # RNN 版 CEB 回归（连续 y 条件先验）
python train.py --task stsb --backbone rnn --model fgib  # RNN 版 FGIB 回归（固定 RFF 连续锚点先验）
python train.py --task housing                          # California Housing 回归（MLP 基线）
python train.py --task housing --model vib              # VIB 回归
python train.py --task housing --model ceb              # CEB 回归（连续 y 条件先验）
python train.py --task housing --model fgib             # FGIB 回归（固定 RFF 连续锚点先验）
python train.py --task agedb --model cnn                # AgeDB 人脸年龄回归（CNN 基线，RGB 64×64）
python train.py --task agedb --model mlp                # AgeDB 人脸年龄回归（MLP 基线）
python train.py --task agedb --backbone cnn --model vib # CNN 版 VIB 回归（ceb/fgib/svib/nib/dvcca 同理）
python train.py --task agedb --backbone cnn --model fgib  # CNN 版 FGIB 回归（固定 RFF 连续锚点先验）
python train.py --model mlp --epochs 20 --batch-size 128 --lr 1e-3 --beta 1e-3
python train.py --model vib --runs 5 --seed 0   # 多 run 取测试集平均指标
python train.py --random-labels --patience 1000   # 随机标签记忆实验（信息自由数据集，看 Train Acc）
python tune.py --model vib ceb fgib --beta 1e-4 1e-3 1e-2 --anchor-scale 1 2 4 8 --parallel 4   # 调参：模型 × beta × anchor-scale 网格并行训练并生成 HTML 结果表（--parallel 为单卡并行数，自动检测多 GPU）
```

- 没有测试套件、linter 配置或 requirements.txt。环境：Python 3.12，torch 2.9.0+cu128，torchvision 0.24.0，scikit-learn 1.7.2（CUDA 可用）。
- 完整参数见 `train.py` 的 argparse：`--task`（默认 mnist（MNIST 分类）；housing 为 California Housing 回归、仅 MLP 骨干；stsb 为 STS-B 文本相似度回归、仅 RNN 骨干；imagenet100 用预训练 ResNet50 特征、仅 MLP 骨干；cora 仅 GNN 骨干；zinc 为 ZINC-12k 分子图回归、仅 GNN 骨干；agedb 为 AgeDB 人脸年龄回归（RGB 64×64）、MLP/CNN 双骨干；imdb/agnews 仅 RNN 骨干，其中 agnews 用 BERT 逐 token 特征）、`--backbone`（默认 mlp；`--model` 为 mlp/cnn/gcn/rnn 时被忽略）、`--hidden-dims`（默认 512 256，MLP/GNN 的各层维度；RNN 首元素为词嵌入维度、列表为逐层 LSTM 隐层维度，层数 = 列表长度）、`--dropout`（0.2）、`--z-dim`（256，仅 VIB 系与 NIB）、`--max-len`（250，IMDb/STS-B；stsb 两句子各自截断到 (max_len-1)//2 后以 [SEP] 拼接）、`--random-labels`（随机化训练标签，固定 seed 42，仅分类任务；信息自由数据集记忆实验）、`--beta`（KL 权重，1e-3；svib 下惩罚为 β·KL²，β 量纲与 VIB 不同，需用 tune.py 扫参）、`--anchor-scale`（4.0，仅 fgib；固定锚点先验均值的尺度，0 为 N(0,I) 先验；分类为各类正交锚点距离、回归为锚点球面半径）、`--patience`（早停，10）、`--data-dir`、`--save-path`、`--log-path`、`--no-save`（不保存 checkpoint，最优模型以 CPU 拷贝保留在内存中、测试评估时直接 load_state_dict）。zinc 的 `--batch-size` 未显式指定时默认覆写为 128（512 批拼块对角邻接约 566MB）；cora 的 `--lr` 未显式指定时默认覆写为 0.01（1e-3 下全图批训练收敛过慢，显式指定时尊重用户值）。

## 架构

**模型**：四个骨干各有一组同构的变体（基线/VIB/SVIB/CEB/FGIB/NIB/DVCCA，DVCCA 仅 MLP/CNN/GNN），接口约定相同（见下）。

- MLP 骨干（`model/mlp/`；`model/__init__.py` 从 `.mlp` 重导出，`train.py` 的 `from model import ...` 接口不变）：
  - `MLP`：纯基线，`forward(x)` 只返回 logits。
  - `VIB`：编码器 h → mu/logvar 头 → 重参数化采样 z → 分类器；先验为固定 N(0, I)。
  - `SVIB`（Squared-IB，Rodríguez Gálvez et al. ICLR 2019 "Caveats"）：`VIB` 的子类（`model/mlp/svib.py`），仅重写 `forward`：调用 `super().forward` 后把 kl 平方返回，即损失等价 **CE + β·KL²**。squared-IB functional（论文 Eq. 8）为 max I(Y;T) − β·I(X;T)²，压缩项取平方；动机：分类任务 H(Y|X)=0 时标准 IB Lagrangian 只能取到硬聚类端点、无法扫出压缩-精度曲线，平方压缩项可恢复整条 IB 曲线。主要面向分类任务；β 量纲与 VIB 不同（KL²），需扫参。继承 VIB 的置零初始化约定。
  - `NIB`（Nonlinear IB，Kolchinsky et al., Entropy 2019；论文见 4.pdf）：独立模型类（`model/mlp/nib.py`），编码器 → `bottleneck_head` 映射到 z_dim 维表示 f(x)，训练时注入同方差高斯噪声 m = f(x) + ε（ε~N(0, noise_var·I)，`stochastic=False` 时不注入）；**无 mu/logvar 头、无任何先验**，压缩项用成对距离的非参数上界 Î 估计 I(X;M)：Î = −(1/N)Σᵢ ln[(1/N)Σⱼ exp(−‖fᵢ−fⱼ‖²/(2·noise_var))]（论文 Eq. 10/16 的同方差特例，`model/mlp/utils.py:nib_mi_upper_bound`，自然对数/nat，论文用 log2/bit，β 差 ln2 倍）。H(M|X) 为噪声熵常数，故 Î 是 I(X;M) 的有效上界，簇充分分离时精确。损失为 CE + β·Î。σ² **为可学习参数**（`log_noise_var`，log 空间保证正值、clamp [-10,10]，初始 log σ²=0 即 σ²=1 = 论文初始值；无 CLI 参数）。β 建议范围：分类 1e-3~2（论文，bit 单位）。注意：初始 σ² 过小时 Î 逼近 ln N、压缩项梯度指数消失（论文 Sec. IVA 的警告），论文按 d=5 瓶颈设定。
  - `DVCCA`（监督 β-DVCCA，Abdelaleem et al., JMLR 2025 "Deep Variational Multivariate Information Bottleneck"，论文见 5.pdf；仅 MLP/CNN/GNN，RNN 因文本重建无意义而禁用）：DVMIB 框架（L = I_encoder − β·I_decoder，编码器图压缩、解码器图生成）的 β-DVCCA（论文 Eq. 18）在监督设置（Y=标签）下的退化形式：压缩项 Ĩ_E(X;Z) = KL(q(z|x)‖N(0,I))（与 VIB 同），解码器项 Ĩ_D(Y;Z) = 分类 CE、Ĩ_D(X;Z) = 输入重建（高斯解码器 → MSE，`decoder` 为 z → 输入空间的小 MLP）。损失 = **CE + β·(KL + MSE_recon)**，β 同时加权压缩与重建（与论文 β 互为倒数重参数化，扫 β 等价）。forward 返回三元组 **(logits, kl, recon_loss)**，`run_model` 统一返回 `(logits, kl, recon)`（其他模型 recon=None），四个损失点对 recon 同样乘 β。GNN 版重建项在全图节点上（转导设置下节点特征无标签、无泄漏），KL 仍按 mask。论文原版 DVSIB 为双视图无监督方法（两视图 = 旋转缩放/Perlin 噪声图像对 + MINE 跨视图项），本项目未实现（见 5.pdf Fig. 2 的实验协议）。
  - `CEB`：条件熵瓶颈（Fischer 2020），同 VIB，但先验改为可学习的 `prior_net`（反向编码器 b(z|y)，无激活线性层，分类时 one-hot 标签、`continuous_y=True` 时连续 y → (mu_p, logvar_p)），KL 为 KL(q(z|x) || r(z|y))；目标 VCEB = KL + γ·CE，项目损失约定下等价 CE + β·KL（β = 1/γ）。本实现为对角高斯版本（Fischer 2020 原版为全协方差小瓶颈 D=4，对角化使瓶颈可扩展到高维 z）。
  - `FGIB`：固定几何信息瓶颈，确定性主路（h 直接分类，z 不参与前向）+ 旁路标签条件 KL（梯度只通过共享编码器回传作为正则），标签条件先验为 `register_buffer` 的固定锚点先验（不参与梯度），KL 无法靠移动先验减小，只能靠 z 对齐锚点，正则力持续存在。**分类**（`continuous_y=False`）：`build_anchor_prior`（`model/mlp/utils.py`）对随机矩阵做 QR 分解得到 K 个正交方向，缩放 `anchor_scale` 作为各类先验均值、方差固定为 `anchor_var`（1.0），要求 `num_classes <= z_dim`（imagenet100 的 100 ≤ 256 满足）。**回归**（`continuous_y=True`，housing/MLP 与 stsb/RNN）：`ContinuousAnchorPrior` 用固定随机傅里叶特征 mu_p(y) = anchor_scale·√(2/z_dim)·cos(2π·ω·y+b)，ω~N(0,2²)、b~U[0,2π) 固定 buffer（零参数，受 run seed 控制），锚点近似落在半径 anchor_scale 的球面上，anchor_scale 语义与分类版一致。两种模式 `anchor_scale=0` 均退化为 N(0,I) 先验。
- CNN 骨干（`model/cnn/`）：同名七个变体 `CNN`/`VIB`/`SVIB`/`CEB`/`FGIB`/`NIB`/`DVCCA`（`SVIB` 为 CNN 版 `VIB` 的子类，`model/cnn/svib.py`；`NIB`/`DVCCA` 独立类，`model/cnn/{nib,dvcca}.py`；DVCCA 解码器输出与展平输入比较算 MSE），结构与 MLP 版一一对应，仅把 MLP 隐藏层换成卷积特征提取器（`build_cnn_encoder`：Conv3x3+ReLU+MaxPool2x2 堆叠，默认通道 (32, 64)，28×28 → 64×7×7=3136 展平后接 Linear 到 hidden_dim=256）；瓶颈头与先验部分和 MLP 版完全一致，`reparameterize`/`kl_divergence` 复用自 `model/mlp/utils.py`。所有 CNN 类新增 `input_size` 参数（默认 28 不变；AgeDB 传 64，64×64、(32,64) 通道 → 64×16×16=16384 维展平），并传入 `build_cnn_encoder` 计算展平维度——此前仅 DVCCA 持有该参数且未传给编码器（非 28 尺寸下展平维度不匹配的潜在 bug），已修复；`input_channels`（默认 1，AgeDB 传 3）同理。CNN 版 `CEB`/`FGIB` 支持 `continuous_y`（AgeDB 回归，同 GNN 版：`prior_net` 输入 1 维 / `ContinuousAnchorPrior` 固定 RFF 锚点条件在 MinMax 缩放后的连续 y 上）。因与 MLP 版类名冲突，顶层 `model/` 只重导出 `CNN`，CNN 版 VIB 系列需 `from model.cnn import VIB` 等导入。
- GNN 骨干（`model/gnn/`，Cora 转导式节点分类 + ZINC 图级回归）：同名七个变体 `GCN`/`VIB`/`SVIB`/`CEB`/`FGIB`/`NIB`/`DVCCA`（`SVIB` 为 GNN 版 `VIB` 的子类，`model/gnn/svib.py`；`NIB`/`DVCCA` 独立类，`model/gnn/{nib,dvcca}.py`，NIB 的 Î 在 mask 节点上计算、DVCCA 的 KL 按 mask 而重建项在全图），`forward` 多收 `adj_norm`（对称归一化邻接，稠密张量）、`mask` 与 `batch` 参数；CEB/FGIB 用 `kl_divergence_masked`（`model/gnn/utils.py`）把 KL 限制在 train mask 节点上，防止标签条件先验接触验证/测试节点标签（标签泄漏）。**图级模式**：`batch` 为节点→图索引（`collate_zinc` 的 repeat_interleave 生成，覆盖 0..B-1），非 None 时（zinc）在 GCN 编码器后插入 `graph_readout`（`model/gnn/utils.py`：index_add_ 求和 + bincount 计数做 **mean 池化**，CUDA 无同步），所有头/先验/分类器都作用在图级表示 (B,·) 上，KL 按图平均（mask=None）；DVCCA 的图级 z 经 `z[batch]` 广播回各节点再解码重建（重建 MSE 按节点加权）；NIB 的 Î 在图级瓶颈表示上计算。`batch=None`（cora）时行为与原节点级路径完全一致。`pooling` 为 `__init__` 参数（默认 "mean"，train.py 对 zinc 显式传 `pooling="mean"`，同 stsb 传 `pooling="max"` 的先例）。GNN 版 CEB/FGIB 支持 `continuous_y`（zinc 回归，`num_classes=1`，先验条件在 MinMax 缩放后的连续 y 上；CEB `prior_net` 输入 1 维、FGIB 用 `ContinuousAnchorPrior`，与 mlp/rnn 版同构）。
- RNN 骨干（`model/rnn/`，IMDb 情感二分类 / AG News BERT 特征分类 / STS-B 文本相似度回归）：同名六个变体 `RNN`/`VIB`/`SVIB`/`CEB`/`FGIB`/`NIB`（`SVIB` 为 RNN 版 `VIB` 的子类，`model/rnn/svib.py`；`NIB` 独立类，`model/rnn/nib.py`；**无 DVCCA**，文本重建无意义、argparse 直接报错），瓶颈头与先验部分和 MLP 版完全一致，仅编码器换成 LSTM 文本编码器（`build_rnn_encoder`：Embedding(vocab_size, hidden_dims[0], padding_idx=0) → 多层 LSTM（层数与各层隐层维度由 `--hidden-dims` 列表决定，默认 (512, 256) 两层，逐层 feed 前一层的 packed 输出，除末层外每层输出后接 Dropout）→ 读出 → Dropout → h (B, hidden_dims[-1])）。**读出方式**由 `pooling` 决定：`"last"`（默认，取末层 `h_n[-1]`，imdb/agnews 用）、`"max"`/`"mean"`（末层输出按时间掩码池化，stsb 用 `"max"`）。**两种输入模式**：`input_dim=None`（IMDb/STS-B）时输入为 (B, L) 的 token id 张量（pad_idx=0 后填充），长度由 `(x != pad_idx).sum(1)` 计算；`input_dim=768`（AG News）时跳过 Embedding，输入为 (B, L, 768) 连续向量序列（BERT 逐 token 特征，全零向量后填充），长度由零向量检测（`(x.abs().sum(-1) > 0).sum(1)`）计算；均 pack 后送入 LSTM（lengths 需 `.cpu()`）。`pretrained_emb` 给出时嵌入层以预训练词向量初始化并**冻结**（默认 None = 随机初始化且可训练，imdb/agnews 不受影响；stsb 用 GloVe）。STS-B 回归时 `CEB`/`FGIB` 支持 `continuous_y=True`（`num_classes=1`，连续 y → `prior_net` / RFF 锚点映射），`FGIB` 的连续锚点复用 `model/mlp/utils.py:ContinuousAnchorPrior`。RNN 版 VIB 系列同样需 `from model.rnn import VIB` 等导入。

**统一前向接口**（`train.py:run_model`）：所有模型返回 `(logits, kl, recon)`，基线返回 `kl=None`、`recon=None`；VIB/SVIB/CEB 额外接收 `stochastic` 标志（训练 True 采样，评估 False 用 z=mu），NIB 的 `stochastic` 控制噪声注入（训练 True 注入 ε，评估 False 不注入），GNN 版另传 `adj_norm`/`mask`。损失 = CrossEntropy + beta·kl + beta·recon（两者为 None 时省略；SVIB 的 kl 已是平方值故其损失即 CE + β·KL²；NIB 的 kl 即 Î 上界故其损失即 CE + β·Î；DVCCA 的 recon 为重建 MSE 故其损失即 CE + β·KL + β·MSE_recon，train.py 损失代码无需区分模型）。FGIB 训练路径必须传入 labels（否则 kl=None 退化为纯基线）。

**关键约定**：

- 所有 VIB 系模型的 logvar 头和 `prior_net` 做置零初始化，使训练起点 KL≈0、sigma=1 —— 修改模型初始化时需保持这一点。FGIB 例外：先验锚点均值非零，置零 logvar 头下训练起点 sigma=1、KL ≈ 0.5·anchor_scale²。
- `model/mlp/utils.py` 提供共享工具：`flatten`（(B,C,H,W)→(B,C·H·W)）、`build_hidden_layers`（Linear+ReLU+Dropout）、`reparameterize`、`kl_divergence`（对角高斯闭式解，logvar clamp 到 [-10, 10]，逐样本求和后 batch 平均）、`build_anchor_prior`（FGIB 固定锚点先验表构造，QR 正交方向）；CNN/RNN/GNN 模型跨包复用其中的工具。
- CEB/FGIB 在 `labels is None` 时返回 `kl=None`（评估或推理时无标签），训练路径必须传入 labels。
- 回归时 CEB/FGIB 用 `continuous_y=True`、`num_classes=1`（输出 (B,1)）；先验条件在归一化后的连续 y 上（housing/stsb/zinc/agedb 标签为 (B,) 一维浮点，`labels.float().unsqueeze(-1)` → `prior_net` / RFF 锚点映射），仍保持置零初始化。回归任务为 housing（MLP 骨干）、stsb（RNN 骨干）、zinc（GNN 骨干，图级回归、mean 图读出）与 agedb（MLP/CNN 双骨干，RGB 64×64 人脸年龄回归）；housing/imagenet100 仍拒绝 CNN 骨干（argparse 直接报错）。

**训练流程**（`train.py`）：

- 数据（分类）：MNIST，训练集 60k 切出 10k 验证（`random_split`，generator 固定 seed 42）。
- 数据（imagenet100）：预训练 ResNet50 特征的 npz，60/20/20 分层划分 + StandardScaler（仅训练集拟合），仅 MLP 骨干。
- 数据（cora）：全图批转导训练，无 DataLoader（`datasets/cora.py`，划分固定 seed 42）。
- 数据（zinc）：DGL 托管的 benchmarking-gnns 官方预处理 pickle `https://data.dgl.ai/dataset/benchmarking-gnns/ZINC.pkl`（约 60.4MB，需 `source /etc/network_turbo` 代理，缓存 `./data/zinc/`，缺文件自动重下）。pickle 为 **protocol 3**、内嵌 `data.molecules.MoleculeDGL`/DGLGraph 对象（本环境无 dgl 且用不到），加载用自定义 Unpickler（`datasets/zinc.py:_ZincUnpickler`：不可导入的全局引用用 `_DummyClass` 占位类恢复——含元类 `__getattr__` 以支持 `getattr(cls, '_reconstruct_scheme')` 式重建，torch/numpy 引用走真实导入），只取各数据集的 `data` 属性（分子 dict 列表）。官方划分 10000/1000/1000 固定（论文可比，不自行切分）；每个分子 dict 含 num_atom（图 9~37 原子）、atom_type（值域 [0,28)）、bond_type（值域 [0,4)）、logP_SA_cycle_normalized（惩罚 logP 回归目标，训练集 std≈2.0）。图转换：节点特征 = atom_type 的 one-hot（28 维 float32），**边特征 bond_type 不使用**（与 benchmarking-gnns GCN 基线一致）；邻接由 bond_type≠0 对称化去重 + 自环 + D^-1/2(A+I)D^-1/2 归一化（同 cora 配方，稠密）；处理结果缓存 `data/zinc/zinc_processed.npz`（object 数组）。collate 用 `torch.block_diag` 拼块对角邻接 + `batch_idx`（repeat_interleave），返回 (x, adj_block, batch_idx, y) 四元组；目标 MinMaxScaler 归一化到 [0,1]（仅训练集拟合），MAE 用 y_scaler 逆归一化回惩罚 logP 单位（GCN 基线 10 epoch 测试 MAE≈0.74/R²≈0.62；文献 ≈0.46，随机基线 ≈1.4）。默认 batch-size 128（未显式指定时覆写）。仅 GNN 骨干（ceb/fgib 走 continuous_y）；训练/评估用专用 `train_one_epoch_zinc`/`evaluate_zinc`（图批四元组 + `run_model(..., adj=, batch=)`，回归语义同 housing/stsb）。
- 数据（imdb）：斯坦福官方 aclImdb_v1.tar.gz（官方 train/test 各 25k 已标注影评）合并后 60/20/20 分层划分（seed 42）→ 30k/10k/10k；词表仅从训练集构建（pad_idx=0、unk_idx=1 保留位，其余按词频编号，约 9 万词）；去 HTML 标签后纯字母分词、截断到 `--max-len`（500）；collate 用 `pad_sequence` 后填充。二分类评估时 `evaluate` 把 (n,2) 概率取第 1 列再算 AUC（sklearn 二分类要求 (n,)）。
- 数据（agnews）：冻结 bert-base-uncased 提取的逐 token **倒数第三层（layer 10）**隐状态（`datasets/extract_agnews_features.py`，`--layer` 可选 1..12，需 `source /etc/network_turbo` 代理 + `HF_HUB_DISABLE_XET=1`），127.6k × 64 × 768 fp16 存于 `data/agnews/agnews_bert_base_layer{layer}_features.npy`（全零向量后填充，长度由零向量检测）；加载端 60/20/20 分层划分 + RAM 预载 fp32（`datasets/agnews.py`，layer 默认 10）。注意：本地 `datasets/` 包与 HF `datasets` 库同名，提取脚本里先移除脚本目录再导入 HF 库。**PCA 降维（自适应，无 CLI 参数）**：`datasets/reduce_agnews_features.py --dim 50` 基于已提取特征做 token 级 PCA（仅训练划分 token 拟合，GPU 分块变换，无需重跑 BERT），生成 `agnews_bert_base_layer10_pca50_features.npy`（12.5GB → 0.8GB）；加载端自动检测该文件——存在则用（input_dim=50）、否则回退原始 768 维（`datasets/agnews.py` 的 `PCA_DIM=50` 常量与 reduce 脚本默认值一致）。效果：fp32 RAM 25.5GB → 1.6GB（30 并行试验约 50GB）；每 epoch GPU 搬运 15GB → 1GB，单 epoch 约 12s → 3.2s；10 epoch 验证 acc 0.936 → 0.922（与 dim 100 的 0.919 相当，小维度下模型收敛更快）。
- 数据（stsb）：HuggingFace GLUE 的 stsb 配置（nyu-mll/glue，datasets-server 的 parquet 分片），下载缓存在 `./data/stsb/`（下载需 `source /etc/network_turbo` 代理，缺文件时 `datasets/stsb.py` 会自动重下）。官方 train/dev（共 7249 条已标注句子对，相似度 0~5；test 分片无标签、GLUE 评测服务器专用，不使用）合并后 **60/20/20 划分**（seed 42，回归不分层，同 IMDb 约定）→ 4349/1450/1450。两个句子分别去 HTML 标签后纯字母分词、各自截断到 `(max_len-1)//2` 后以 [SEP]（idx=2）拼接成单条序列；词表仅从训练集构建（pad_idx=0、unk_idx=1、sep_idx=2 保留位，其余按词频编号 3..V+2，约 1 万词）；目标 MinMaxScaler 归一化到 [0,1]（仅训练集拟合），MAE 用 y_scaler 逆归一化回原始 0~5 分。**词向量**：GloVe 6B 100d（自动下载 glove.6B.zip 约 820MB 并解出 txt 缓存，HF 源需代理），嵌入层以 GloVe 初始化并**冻结**（小数据集上微调词向量会迅速过拟合，实验验证：微调 R²≈0.05 vs 冻结 0.27）；stsb 默认读出用 **max 时间池化**（train.py 传 `pooling="max"`，末时刻状态对相似度任务偏差大：max 池化 Pearson≈0.5 vs 末状态 0.28）。传 `pretrained_emb` 时**词嵌入维度取自矩阵宽度（100）**，`--hidden-dims` 仅作逐层 LSTM 隐层维度（不再含嵌入维度）；未显式指定时 stsb 默认**单层 LSTM 256**（多层在小数据集上过拟合，实验验证：单层 val R²≈0.27 vs 两层 0.07）。仅 RNN 骨干（vib/svib/nib 回归即用，ceb/fgib 走 continuous_y）。
- 数据（agedb）：GitHub HarmonLiu05/agedb 完整镜像 `https://media.githubusercontent.com/media/HarmonLiu05/agedb/main/agedb_images.tar.gz`（约 171.5MB，16,488 张图、567 人、年龄 0~96，需 `source /etc/network_turbo` 代理，缓存 `./data/agedb/`：tar 包 + 平铺解压目录 `agedb_images/`，缺文件自动重下）。年龄标注嵌在文件名中（官方命名 `{id}_{name}_{age}_{fm}.jpg`），`_extract_ages` 用正则 `_(\d+)_([fm])\.jpg$` 全量提取并做覆盖校验（不匹配直接报错，不静默过滤；越界检查宽松 0~120）。预处理：RGB 64×64 + ImageNet 均值/标准差归一化（`datasets/agedb.py`）。**预解码缓存（性能关键）**：首次加载把全部图像解码为 (16488, 64, 64, 3) uint8 数组（约 202MB，`_load_image_array`）并缓存 `data/agedb/agedb_64x64_uint8.npy`（行数与排序文件名顺序一致，不匹配自动重解码），之后每次加载直接读缓存（瞬时）、每 epoch 只做微秒级张量 ToTensor+Normalize——数据迭代从 6.7s/epoch 降到 0.6s/epoch（GPU 训练本身仅 ~0.6s/epoch，此前 94% 时间耗在单进程 JPEG 解码）；同 agnews RAM 预载 / zinc 处理缓存惯例。全部合并 60/20/20 划分（seed 42，回归不分层）→ 9892/3298/3298；目标年龄 MinMaxScaler 归一化到 [0,1]（仅训练集拟合），MAE 逆归一化回岁（均值预测基线 MAE≈14.2 岁）。MLP/CNN 双骨干：mlp 传 `input_dim=12288`、cnn 传 `input_channels=3, input_size=64`（ceb/fgib 走 continuous_y）；训练走通用回归路径（`task in ("housing", "stsb", "agedb")`），按验证 R² 早停。
- 数据（回归）：`fetch_california_housing`（20640 样本、8 特征），60/20/20 划分（seed 42），特征用 StandardScaler 标准化、目标用 MinMaxScaler 归一化到 [0,1]（均仅在训练集上拟合）。损失 MSELoss 在归一化目标上计算，MAE 用 y_scaler 逆归一化回原始单位（十万美元；R² 与量纲无关），按验证 R² 早停。housing/imagenet100 拒绝 CNN 骨干（argparse 直接报错）。stsb/housing/agedb 共用同一回归训练/评估路径（train.py 中 `task in ("housing", "stsb", "agedb")` 判回归；zinc 走专用图批函数）。
- 每 run 使用 seed+i-1；分类按验证集宏平均 AUC、回归按验证 R² 早停（patience=10），保存最优 state dict 到 `output/<name>/<name>_run{i}.pt`，训练日志写入 `output/<name>/train_<name>.log`（logging 同时输出到控制台和文件）。训练日志每 epoch 报告 Train Loss + **Train Acc**（回归无 Acc）——随机标签实验靠 Train Acc 爬升识别记忆（val/test 始终 ~随机水平；MLP 约 40 epoch 开始记忆，实验时用 `--patience 1000` 避免早停）。开启 `--random-labels` 时结束后额外报告所有 run 的**最终 epoch Train Acc 均值±标准差**。`<name>` 按 `{dataset}_{backbone}_{model}` 命名（基线为 `{dataset}_{model}`），dataset 为 `mnist`/`imagenet100`/`cora`/`imdb`/`california`/`stsb`/`zinc`/`agedb`，如 `output/mnist_mlp_vib/`、`output/mnist_cnn_vib/`、`output/cora_gnn_vib/`、`output/imdb_rnn_vib/`、`output/california_mlp_ceb/`、`output/stsb_rnn_fgib/`、`output/zinc_gnn_vib/`、`output/agedb_cnn_vib/`——每个 任务×骨干×模型 组合对应唯一目录。
- 早停结束后加载最优 checkpoint 在测试集评估（分类报告 Loss/Acc/宏平均 AUC/Precision/Recall，回归报告 Loss/MAE/R²，其中 Loss 含 beta·KL 项、MAE/R² 为纯预测指标），最后对全部 runs 取均值±标准差。
- 调参脚本（`tune.py`）：复用 `train.py` 的 `build_parser()` 与 `get_dataset_name()`（train.py 的 argparse 与数据集命名均已提取为函数），`--model`/`--beta`/`--anchor-scale` 均为 nargs="+" 列表：基础模型（mlp/cnn/gcn/rnn）无 beta/anchor 维度（每模型 1 组），vib/ceb/svib/nib/dvcca 仅 beta 维度，fgib 为 beta × anchor-scale 两维，总试验数 = 各模型组合数之和；`--parallel`（默认 2）为**每张 GPU** 的并发子进程数（`sys.executable train.py ...`，cwd 为项目根目录，组合按轮转进入各 GPU 的**独立队列**——每卡一个线程池、并发严格 ≤ `--parallel`、互不影响，经子进程环境变量 `CUDA_VISIBLE_DEVICES` 指定，可用 `CUDA_VISIBLE_DEVICES` 限制参与调参的 GPU；无 GPU 时回退单 CPU 槽位），总并行 = GPU 数 × `--parallel`；每组参数在 `--results-dir`（默认 `tune_results/`）下一个独立子文件夹 `{dataset}_{backbone}_{model}_beta_{b:g}_anchor_{a:g}/`（按模型类型省略无维度后缀，基线无 backbone 段，如 `mnist_mlp/`、`mnist_mlp_vib_beta_0.001/`、`mnist_cnn_fgib_beta_0.001_anchor_4/`；仅含 `train.log`——tune.py 恒传 `--no-save`、不保存模型参数（最优模型保留在内存中用于测试评估），失败时写 `error.log` 并在表格标 FAILED）；全部完成后解析各日志 `Average over N runs | Test ...` 汇总行生成 HTML 结果表（**每个模型一张表格**，各表带排序下拉框可自由选择排序指标——JS 按行 data 属性排序，Acc/AUC/Pre/Rec/R² 降序、Loss/MAE 升序，默认 Acc（回归 R²）、绿色高亮各模型默认指标最佳行，失败组合沉底标 FAILED；基础模型行 beta/anchor 显示 "-"；文件名单模型为 `{dataset}_{backbone}_{model}_tune_results.html`、多模型为 `{dataset}_{backbone}_{m1+m2}_tune_results.html`）。模型列表无 fgib 且 anchor-scale 列表多于 1 个值时打印警告。
- 评估指标用 sklearn 计算（`roc_auc_score(multi_class="ovr")`、`precision_score`/`recall_score` 均 macro 平均、`mean_absolute_error`/`r2_score`），因此 `evaluate` 需在 no_grad 下收集整个 loader 的预测。
- 数据集：MNIST 在 `./data/MNIST/`；California Housing 缓存在 `./data/california_housing/cal_housing_py3.pkz`，训练时以 `--data-dir` 下的 `california_housing/` 子目录作为 sklearn 的 data_home 加载（数据来自 OpenML 44977，与 sklearn 官方数据一致）。figshare 源在本机被墙，`~/scikit_learn_data/cal_housing_py3.pkz` 留有原始备份，项目内副本丢失时可从备份恢复。IMDb 下载解压缓存在 `./data/imdb/`（下载源 ai.stanford.edu 本机可达；数据管道与词表构建见 `datasets/imdb.py`）。STS-B parquet 分片缓存在 `./data/stsb/`（源为 HF nyu-mll/glue 的 datasets-server 链接，需代理，缺文件自动重下）。ZINC pkl 与处理结果缓存在 `./data/zinc/`（`ZINC.pkl` + `zinc_processed.npz`，源 data.dgl.ai，需代理，缺文件自动重下）。AgeDB tar 包与解压图像缓存在 `./data/agedb/`（`agedb_images.tar.gz` + `agedb_images/`，源 GitHub media CDN，需代理，缺文件自动重下）。
